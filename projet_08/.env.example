# Copier en .env (jamais commité) et adapter si besoin.
# Le port doit correspondre à celui publié par docker-compose.yml
# (le premier des deux dans "ports: - HOTE:CONTENEUR", ici 5433).
DATABASE_URL=postgresql://support_user:SupportDev2026@localhost:5433/support_client
